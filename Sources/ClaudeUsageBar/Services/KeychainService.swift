import Foundation

/// Placeholder — no longer used. OAuth credentials are not needed
/// since the app now reads from the cache file written by Claude Code hooks.
/// Kept for potential future use.
struct KeychainService {
    // Intentionally empty — all data now comes from ~/.claude/usage_cache.json
}
